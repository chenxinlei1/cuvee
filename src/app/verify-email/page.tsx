import Link from "next/link";
export default async function VerifyEmailPage({searchParams}:{searchParams:Promise<{token?:string;verified?:string;error?:string}>}){const p=await searchParams;if(p.token) return <meta httpEquiv="refresh" content={`0;url=/api/auth/verify-email?token=${encodeURIComponent(p.token)}`}/>;const ok=p.verified==="1";return <main className="container mx-auto flex min-h-[calc(100vh-4rem)] max-w-lg items-center px-7 py-12"><section className="card-lg w-full p-8"><p className="kicker">Account security</p><h1 className="mt-3 font-serif text-4xl">{ok?"Email verified":"Verification link unavailable"}</h1><p className="text-soft mt-3 text-sm">{ok?"Your email is verified. You can sign in after an administrator approves your account.":"This verification link is invalid, expired, or has already been used."}</p><Link href="/login" className="mt-6 block text-sm underline">Go to sign in</Link></section></main>}
