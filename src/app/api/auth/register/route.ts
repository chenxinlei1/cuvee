import { NextResponse } from "next/server";
import { z } from "zod";
import { createUser, writeAuditLog } from "@/lib/auth/db";
const Body=z.object({name:z.string().trim().min(2).max(80),email:z.string().email(),password:z.string().min(10).max(128),organizationType:z.enum(["chateau","negociant","distributor","buyer"]),organizationName:z.string().trim().min(2).max(120)});
export async function POST(request:Request){const parsed=Body.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Invalid registration details"},{status:400});try{const user=await createUser({...parsed.data,role:"buyerStaff",status:"pending"});await writeAuditLog(user.id,"auth.register","user",user.id);return NextResponse.json({message:"Registration submitted for platform admin approval."},{status:201});}catch(error){return NextResponse.json({error:error instanceof Error&&error.message==="EMAIL_EXISTS"?"Email already registered":"Registration failed"},{status:error instanceof Error&&error.message==="EMAIL_EXISTS"?409:500});}}
